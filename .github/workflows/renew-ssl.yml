name: Renew SSL Certificate

on:
  schedule:
    - cron: '0 2 1,31 * *'
  workflow_dispatch:

jobs:
  renew:
    runs-on: ubuntu-latest
    steps:
      - name: Install acme.sh
        run: curl https://get.acme.sh | sh -s email=admin@cheapgo.top

      - name: Install aliyun CLI
        run: |
          curl -fsSL "https://aliyuncli.alicdn.com/aliyun-cli-linux-latest-amd64.tgz" -o aliyun-cli.tgz
          tar xzf aliyun-cli.tgz
          sudo mv aliyun /usr/local/bin/

      - name: Configure credentials
        env:
          ALIYUN_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
          ALIYUN_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
        run: |
          aliyun configure set --mode AK \
            --access-key-id "$ALIYUN_ACCESS_KEY_ID" \
            --access-key-secret "$ALIYUN_ACCESS_KEY_SECRET" \
            --region "cn-shenzhen"
          echo "Ali_Key=$ALIYUN_ACCESS_KEY_ID" >> ~/.acme.sh/account.conf
          echo "Ali_Secret=$ALIYUN_ACCESS_KEY_SECRET" >> ~/.acme.sh/account.conf

      - name: Issue/Renew Certificate (RSA 2048)
        run: |
          ~/.acme.sh/acme.sh --issue \
            -d appliance-api.cheapgo.top \
            --dns dns_ali \
            --keylength 2048 \
            --server letsencrypt \
            --force

      - name: Upload certificate to FC
        run: |
          CERT=$(cat ~/.acme.sh/appliance-api.cheapgo.top/fullchain.cer)
          KEY=$(cat ~/.acme.sh/appliance-api.cheapgo.top/appliance-api.cheapgo.top.key)
          BODY=$(jq -n \
            --arg cert "$CERT" \
            --arg key "$KEY" \
            '{
              "protocol": "HTTP,HTTPS",
              "routeConfig": {"routes": [{"path": "/*", "functionName": "api", "qualifier": "LATEST"}]},
              "certConfig": {"certType": "upload", "certName": "appliance-api-cheapgo", "certificate": $cert, "privateKey": $key}
            }')
          aliyun fc PUT /2023-03-30/custom-domains/appliance-api.cheapgo.top --body "$BODY"

      - name: Verify HTTPS
        run: sleep 10 && curl -sI https://appliance-api.cheapgo.top | head -5
